# WP-1440 — Open v1.5 over the body that landed without it, and ship it

Milestone: v1.5 · Status: 🔄 2026-09-17 — opening the milestone the work
already happened under
Depends on: WP-1439 (the Windows gate it cleared)

## Goal

`docs/milestones/v1.5.md` exists, 26 WPs sit under a `### v1.5` section of the
index, `pyproject.version` reads `1.5.0.dev0`, and `docs/releases/1.5.0.md` is
written from the **tag range** rather than from the milestone. When this WP
closes the only thing between the tree and PyPI is `RELEASING.md` steps 3–7,
which need a green Windows nightly on a `main` commit and a person at the
`pypi` approval.

## Context

**This milestone is opened backwards and the record has to say so.** Protocol
rule 6 wants Scope and Acceptance written *at the open*, and the v1.3 record
says plainly that rows written at ship are the weaker evidence, because a row
written after the fact cannot fail. Here the work landed first: v1.4.0 was
tagged 2026-09-14 16:13 and 496 commits have landed since under no milestone
at all. Every acceptance row this record carries is therefore an at-ship row,
and the record states that in place of pretending otherwise.

**The body, measured from the tag range and not from the dates.** 26 WPs have
commits in `v1.4.0..main`:

- **The live-watcher track**, eighteen rungs: 1401–1406, 1413, 1423–1431,
  1438, and 1439 behind them. `rietx watch`, every fit recording itself, the
  stop verb, the GUI verb on a scratch copy, one palette across three browser
  surfaces.
- **Foreign model files** (1118, 83 commits — the largest single WP in the
  range): four formats read and written.
- **A measured background** (1309), **the report that repeated itself**
  (1310), the manual's link home and its theme (1411, 1412), the WP two
  sessions picked (1422), and the `k` → `stol` symbol audit (1436, 1437).

1408, 1409 and 1410 closed on 2026-09-14 **before** the tag and are v1.4's;
the ROADMAP's "29 WPs" counted by date. 1312, 1319, 1338, 1419 and 1432 each
carry one commit in the range and stay ⬜ — a mailbox write by another
session's handover, not a closure.

**The notes are the work, not the mechanics.** They are written from the tag
range, and v1.4's own pass found two changes shipping with no record entry
behind them. Two measured gaps here:

- Eight entries are staged in `docs/milestones/v1.4.md` §§ Additions/Breaks
  with post-tag dates (1403 ×2, 1118, 1309 for the additions; 1402, 1403,
  1309 ×2 for the breaks). They move to the v1.5 record. The eighteen-rung
  track has **one** staged addition between all of it, and 1118's 83 commits
  have one entry naming `read_gsas_prm`.
- **30 commits in the range carry no WP prefix.** Most are docs, triage, CI
  and the #313 skill pass, which owe no note. Four are behaviour: the CW
  neutron `fwhm_deg` seed and its refusal (#124, two commits), the degenerate
  metric `d_spacings` refuses by name (#283, whose bounds half was reverted in
  the same range), and the joint solver's probe guard. Each needs an entry or
  a written reason it needs none.

**The `k` → `stol` rename (1436) owes no break entry, and that was checked.**
Both renamed signatures — `crystallography.scattering.f0` and
`optimize.qpa.weight_fractions` — are internal by `tests/api_surface.py`'s
internal sentence, and every call site passes positionally. What the notes do
owe is 1437: `help.py` printed an Lp formula 0.508× from the one the code
computes at 2θ = 90°, which is a wrong number a reader could have acted on.

## Non-goals

- **Tagging and publishing.** `RELEASING.md` steps 5–6 are the maintainer's,
  and step 4 needs a Windows nightly green on a `main` commit — the scheduled
  run at ~10:25 UTC on 2026-09-18 is the first that can give one. This WP
  stops at a tree that is ready.
- **Taking queued WPs into the milestone.** v1.5 is the body that landed.
  1433–1435, the magnetic track and the two triages stay unscheduled.
- **A Windows `run.lock` over `msvcrt.locking`**, the candidate 1439 left.

## Tasks

- [ ] Open the milestone: `docs/milestones/v1.5.md` with Scope and Acceptance,
      `pyproject.version` → `1.5.0.dev0`, `metadata.version` in
      `docs/skill/rietx/SKILL.md` to match, `rietx skill --install . --copy`,
      and the reinstall `RELEASING.md` step 1 says comes after
- [ ] Move the 26 WPs: each file's `Milestone:` line → `v1.5`, and the ROADMAP
      rows into a `### v1.5` section mirroring them
- [ ] ROADMAP: the v1.5 row in § Milestones, Current focus rewritten inside
      both caps, the outgoing narrative moved to the v1.5 record per rule 5
- [ ] The notes audit: the eight staged entries moved, the track's and 1118's
      missing entries written, the four unprefixed behaviour commits
      dispositioned
- [ ] `docs/releases/1.5.0.md`, written for a reader upgrading from 1.4.0
- [ ] Skill: none — this WP ships a milestone and adds no diagnostic code,
      correction or task shape (root CLAUDE.md § skill)

## Acceptance

```sh
.venv/bin/python -m pytest tests/test_docs_consistency.py tests/test_skill.py \
  tests/test_manual_api.py -n auto --dist loadgroup
.venv/bin/python -m pytest -n auto --dist loadgroup -m "not slow"
.venv/bin/python -m ruff check src tests examples
```

The release notes are not testable, so the bar is stated instead: every
user-facing change in `git log v1.4.0..main` is named in the notes or has a
written reason it is not, and the 30 unprefixed commits are each
dispositioned in the handover entry.

## References

- `docs/RELEASING.md` — steps 1–7, and why `twine upload` is never run by hand.
- `docs/ROADMAP.md` § Session protocol rules 5 and 6.
- `docs/milestones/v1.3.md` — the precedent for saying that at-ship rows are
  the weaker evidence.
- `docs/milestones/v1.4.md` §§ Additions/Breaks — the eight staged entries.

## Handover log

- **2026-09-17** — created.
