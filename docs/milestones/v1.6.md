# v1.6 — the magnetic structure

**Opened 2026-09-18** (version → `1.6.0.dev0`), the same day v1.5 shipped.
[`../ROADMAP.md`](../ROADMAP.md) § v1.6 carries the rows. The slot opens on
schedule rather than behind its work, which is what the [v1.5
record](v1.5.md) says the previous one cost.

## Scope

The magnetic scattering track: seven WPs, 1326 to 1329, 1343, 1418 and 1419.
Each WP file states its own context and baseline; nothing is restated here.

The track answers a question the package refuses today. Three readers decline
a magnetic structure with one sentence, and the unexplained-intensity report
names a magnetic contribution as a cause it cannot test. The rungs run from
the hypothesis test that needs no moment (1326, a satellite is a position),
through the moment itself (1327) and its interchange (1328), series (1329)
and its price in peak width (1343), to determination rather than statement
(1418) and a nuclear superstructure refined on mode amplitudes (1419).

Most of it has a working reference implementation on the `mustachefeeling`
fork, measured on data this repository does not hold. Issues #256, #257, #277,
#286 and #293 are the design threads, and the decisions taken in them are
recorded in the WP files rather than in the threads.

**Not in scope, and the fence says so.** Neutron time-of-flight stays at v2+
(§ v2+, issue #193), as do incommensurate structures, polarised neutrons and
magnetic X-rays (1327's non-goals). The fork's integration branch carries a
TOF chain merged into the magnetic one; it is split back out rather than
landed here, and the ruling is 1419's `### Inherited`. Magnetic structures
left the v2+ fence on 2026-09-02 by a decision recorded in DESIGN.md, and a
second item leaves it the same way or not at all.

## Acceptance

**These rows are written at the open**, which is what protocol rule 6 asks
for: a row written after the fact cannot fail. Each names a state a later
session can check without asking what was intended.

They are stated at the level `../ROADMAP.md` and the WP files already commit
to. The session that opens each WP sharpens its own row here, because a bar
set by someone who has not read the WP is a bar set too low.

- **A satellite is answered without a moment** (1326). A pattern whose
  nuclear model leaves intensity at G ± k gets the candidate propagation
  vectors and a stated hypothesis test, with no moment anywhere in the model,
  and a k = 0 case says so rather than ranking.
- **An unsupported moment is held, not bounded** (1327). A moment the powder
  cannot see is held for the stage under 1301's rule, `StageResult.held`
  records it, and the report says what the powder cannot separate rather than
  returning a confident wrong singleton.
- **The stored form is the operator list** (1327). A magnetic or child group
  is the operations it has; a Hermann-Mauguin symbol beside them is a label
  and must generate them exactly, or it is refused. A phase whose symbol does
  not generate its own operations gets the wrong absences and converges
  anyway, which is the whole safety property.
- **Two lists, and each consumer names which it reads** (1419). The full
  child group drives reflection generation, multiplicity and the cell metric;
  the sign-consistent stabiliser drives orbit expansion of the mode field
  alone. The fork's sweep is why this is a row: the metric is safe on 32 of
  32 pairs because the dropped operations are translation-coset partners, and
  reflection generation is not — the unique count doubles on the 15 pairs
  where the reduction bites, harmless in Rietveld and wrong in Le Bail and
  Pawley, where the extras would take free intensities.
- **A mode amplitude is absolute, and it has a seed** (1419). χ² is an even
  function of the amplitude vector at a zone-boundary k, so A = 0 is a local
  *maximum* and the gradient there is exactly zero. The amplitude is absolute
  in the ADP and Stephens pattern rather than a coordinate-DOF offset, a seed
  rule in `Stage.strain_seed`'s shape starts it off the parent, and an
  acceptance case starts *at* the parent and converges.
- **The tested quantity is A_τ, not its components** (1419). The per-irrep
  total carries an esd from its own covariance block, the way
  `model/geometry.py` propagates J·Cov·Jᵀ. The components are an arbitrary
  basis's coordinates, so testing each of them asks a question the basis
  chose; the printed sign is a domain convention and says so.
- **A magnetic file opens and writes back** (1328). magCIF in and out, and
  the three readers that refuse a magnetic structure stop refusing.
- **The moment's trajectory is a measurement, not an ordering artefact**
  (1329). A ramp through an onset runs `direction="both"` and flags what the
  two chains disagree on, as every series does.
- **A broad magnetic peak is not paid for by a low moment** (1343). The
  magnetic width has its own term, and the record says what declaring it
  changes — never an Rwp comparison as the evidence.
- **The determination abstains where the powder cannot separate** (1418).
  LaMnO₃ 50 K ranks A-type first of four with its powder-equivalence class
  listed; Cr₂WO₆ 150 K abstains with the k = 0 sentence; every shipped
  fixture is bit-identical with no magnetic model declared.
- **The skill's magnetic rows are in `references/magnetic.md`** (every WP).
  Diagnostic codes included, so `diagnostics.md`'s remaining headroom never
  has to absorb them, and the merge result of the whole set is measured once
  before the first PR is cut rather than at the sixth merge.

## How v1.6 is getting here

### The v1.5.x silent-answer run (moved from ROADMAP, 2026-09-22)

Six fixes landed between the v1.5 ship and v1.6's first WP, none of them
milestone work, all of them a wrong answer nobody could see. 1310's last two on
2026-09-18 — [1434](../wp/1434-the-bound-test-asks-the-wrong-question.md), the
bound flag asking whether the limit carried load rather than whether the value
stopped near one, and [1435](../wp/1435-a-hold-the-caller-declares.md), a
caller's hold outranking a plan's glob. Then on 2026-09-19
[1432](../wp/1432-a-tie-onto-a-rederived-dof.md) and
[1342](../wp/1342-a-freeze-that-reads-names.md), one user tie story from each
end: what a tie accumulates, and what a freeze reading a *name* cannot see.
Then on 2026-09-21/22 [1415](../wp/1415-a-sigma-column-smaller-than-root-y.md)
and [1442](../wp/1442-a-ghost-search-at-chance.md), both about a bar set in σ
answering a question σ cannot answer — a peak census thresholding the
background model's own tracking error, and a contamination screen firing at the
chance rate on patterns whose optics remove the line it looks for. All six are
staged in [releases/1.5.1.md](../releases/1.5.1.md).


### The supercell 1442 unmasked, and the route that does not work (2026-09-22)

1442 did not cause a ranking defect, it uncovered one. With the two wrongly
dropped lines restored, the round-robin brucite row puts an a × 2 supercell
above the certified cell, on one extra indexed line.
[1446](../wp/1446-a-supercell-that-outranks-the-truth.md) built the fix its goal
named, ordering a candidate below any derivative parent whose extra lines the
pattern lacks, and measured that the same rule demotes SRM 676a's own cell,
because `R -3 c`'s c-glide leaves 33 of its 35 in-range extras absent. Every
variant tried fires on that correct cell, so nothing landed and the panel is
unchanged. `ambiguity._refuted_supercell` is kept private and tested as the
instrument that measured it,
[1449](../wp/1449-rank-on-what-the-screen-determined.md) owns the
extinction-screen route, and brucite's strict xfail stands.


### The outgoing Current focus (moved from ROADMAP, 2026-09-18, at the open)

**No milestone is open.** [v1.5](../ROADMAP.md#v15--a-window-into-a-run)
shipped 2026-09-18 ([record](v1.5.md), [notes](../releases/1.5.0.md)), tagged
at `13bce502`, published and on PyPI, so `RELEASING.md` is walked to the end
and `pyproject.version` is `1.5.0`. **Opening v1.6 is the maintainer's
decision**, and the record says what opening one 496 commits late cost. Until
one opens, work lands in § v1.5.x, staged in
[releases/1.5.1.md](../releases/1.5.1.md) the day it lands.

**Nothing is in flight.** [1441](../wp/1441-a-constraint-the-series-can-declare.md)
was the first v1.5.x row; 1432 and 1342 carry what it learned. 1436 closed the
symbol audit 1437 opened and freed `k` for the **magnetic track** (1326-1329,
1418), the largest queued block with a discharged blocker.

### v1.5.x, 2026-09-18 (moved from Current focus on close)

WP-1310's two unanswered questions both closed the same day, and they are the
same shape twice: a flag that was measuring the wrong thing, and a
declaration with nowhere to live. **1434** made `BOUND_HIT` ask whether the
limit carried load rather than whether the value stopped near one. **1435**
gave a caller `hold`/`unhold`, because `vary=False` never survived a plan's
`turn_on` and nothing reported the override — measured at 21.7 ppm on a
certified LaB6 cell, by a fit whose Rwp was the *better* of the two. Both are
staged in [releases/1.5.1.md](../releases/1.5.1.md). What 1435 leaves behind
for the magnetic track and for 1414: `ParameterTable.set_vary` can refuse
now, so read its return rather than the list handed to it.

### v1.5.x, 2026-09-19 (moved from Current focus on close)

**1432** ended a tie that meant more each time it was read. A tie onto an
atomic coordinate's degree of freedom gained its source's whole value at every
verb that wrote the models back, so a position drifted by one displacement per
call while the variable naming it still read what was declared. Two
consequences reached a quoted number: a second `fit()` on one `Refinement`
reported the variable at zero with the structure carrying the displacement in
full, and an antiphase pair came back asymmetric by declaration order.
`replay` carried the same defect alone, which is the worse place for it, since
replay exists to say what a recorded state was. Staged in
[releases/1.5.1.md](../releases/1.5.1.md). What it leaves for the magnetic
track: a mode amplitude can now be written with the machinery `main` already
has, which is the question 1419 was blocked on, and every consumer of the tie
register calls `ParameterTable.rebase_anchored_dofs`.

**1342** closed the same day and is that story from the other end: not what a
tie accumulates, but what a tie makes *invisible*. Two per-stage freezes tested
what a free parameter was called, and a tie moves the freedom to a name they
did not recognise, so the hold on a phase the data cannot see and the Le
Bail/Pawley force-fix each reported success on a set they could not see into.
Five patterns of a ramp containing no fluorite returned its cell spread over
0.296 Å, two values resting on the caller's own bound, with every Rwp inside
1e-5 of the correct run's and three of the five patterns reporting nothing at
all. The reflex fix was refuted by measurement: holding every column that
touches an invisible phase would have frozen the visible phases' cells, 10 441
ppm from the truth against 1 ppm, so a column is held only when *everything* it
moves is invisible. Staged in [releases/1.5.1.md](../releases/1.5.1.md). What
it leaves for the magnetic track and for 1420: `StageResult.held` names a
**column**, which need not be a phase path, and the phase behind one is found
through the new `held_reach`; `ParameterTable.column_reach` is the accessor any
future freeze resting on flatness reads, and `entry_reach` is its answer for an
entry that is not a column.
