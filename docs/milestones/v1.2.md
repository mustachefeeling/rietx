# v1.2 — the GUI for a crystallographer

**Opened 2026-08-25** (version → `1.2.0.dev0`). The slot was drafted as
"free-standing peaks" (1101-1103); those WPs shift to v1.3 and keep their
numbers, the precedent v1.1 set when the speed series took its slot.

## Scope

The maintainer used the GUI the way a first-time crystallographer would and
wrote notes. Three read-only passes over `gui/src`, `src/rietx/gui` and the
docs guards found a concrete cause in code for every note; each cause is one
WP (`../ROADMAP.md` § v1.2). The register of the notes, the cause, and the
verdict:

| Note | Cause found | Verdict | WP |
|---|---|---|---|
| Menu bar has four or five button and chip sizes | five button geometries in the header, three chip geometries across panels, no size tokens, `.tiny` at three sizes, a `.chip`/`.note` specificity collision | accept; the register rule | 1201 |
| Hover shows help cursor, click shows the role, default, common range | `title=` is the only mechanism (169 strings); no description, unit or range exists on a parameter row | accept; corpus + popover | 1202, 1203 |
| Test the GUI without messing up git; example projects for a new user | `suggested_project` is cwd, `.rex` unignored, no `--state-dir` flag, no `.rex` in the repo | accept | 1204 |
| A gentler introduction to the plan | only `when_to_use` is visible; globs never resolved against the table | accept | 1208 |
| Hide peak markers; show candidates through just the data; hide the fitted peaks | no peak trace is toggleable; a candidate draws nothing and carries no predicted 2θ | accept | 1210, 1211 |
| Plot jitters on hover in Peaks, and on exclude | the hover ring is a data-axis trace restyled on an unpinned plot; one exclude drag runs three reacts | accept; measure first | 1212 |
| Flags text, chip sizes, two 2θ values, 1e-17 intensities, ten decimals, on/off column | bare tokens + a diagnostics strip; the collision; `35(111)` from an esd ≥ 1°; `no_intensity` lines still printed; `formatValue` places from a tiny esd; checkbox in the actions cell | accept | 1209 |
| Picked-peak fit and model fit the same colour | different tokens, but the peak layer has no identity: `--accent` = `--plot-diff`, same weight, no legend, drawn in every tab | reframe | 1210 |
| Tooltip covers the data | `x unified` hover; plotly cannot position it | push back on moving it; a readout strip | 1213 |
| Exclude cursor: two lines and shading wanted | plotly's select marquee, unstyled | accept | 1212 |
| Save instrument profile from the GUI | no route; exports are result-gated | accept, model-gated | 1214 |
| Refinement flags in the Model panel | `vary` is on the wire; the join exists; no widget | accept | 1214 |
| Structure section: one row per atom, editable positions, Wyckoff automatic, the line | one to three rows per atom; xyz read-only; letters on a button; `tr.sub` border | accept; typed xyz refused off-subspace | 1215 |
| Instrument fields misaligned; UVW over XYZ | a wrapping flexbox; the select moves the wrap point | accept | 1216 |
| New-project overload; scan; directory; mandatory CIF/instrument; no way to open; wizard sticks | registry prose shown raw; count never shown; text input for a path; hard requirements at every level; `Open…` only with a project open; `wizardOpen` never cleared | accept, in three WPs; the API keeps its refusal, the wizard pre-fills a visible default | 1205, 1206, 1207 |
| Git-style history graph; compare columns ragged; Δ as absolute and % | `edgePath` is one Bézier over the gap; `toPrecision(7)`, silent 200-row cap | accept | 1217 |
| Keep all the manuals in sync with the code | no guard covers GUI docs; prose about behaviour is not checkable | reframe: one authority for descriptions, partitions for names, generated screenshots | 1017 |

Decisions taken at the opening, with the user: help is cursor-only with a
click popover and no visible mark; a project without a CIF is two WPs (a
typed cell, then pattern-only projects); a ~3.2 MB example set ships in the
wheel; chips are non-interactive facts (the space-group adopt chips become
buttons); the hover tooltip becomes a readout strip. Two rules enter
`gui/CLAUDE.md` at the opening: GUI prose is written under
`/yue-docs-style`, and a button's `title=` is a verb phrase while every other
explanation is a corpus entry.

## Acceptance

Each row is checked by the WP that owns it, on the final tree, and quoted
here at ship with venv and platform:

- **One control vocabulary**: the style test (`gui/src/lib/style.test.ts`)
  green; the header shows at most three control geometries (1201).
- **One help mechanism**: no `title=` on a non-button element (vitest);
  every parameter family, flag, stage field and wizard field has a corpus
  entry (`tests/test_help.py`); the glossary builds `-W` (1202, 1203).
- **A project from nothing**: from a blank state, open a project by
  browsing, create one without the CLI argument, create one from a typed
  cell and one with no structure, all in a browser pass (1204-1207).
- **The plot holds still**: the measured table in WP-1212 shows zero axis
  change on hover and on an exclude drag, one react per drag.
- **The Model panel is the editor**: vary flags set there land as
  `set_vary` nodes; an instrument profile round-trips save → load (1214);
  a typed coordinate on a special position is refused with the free
  direction named (1215).
- **The manual is guarded**: routes partitioned, panel names in a corpus,
  screenshots generated, the glossary the one description authority; the
  beta declaration lifted (1017).

## How v1.2 is getting here — the rolling narrative

- **2026-08-25** — opened. The triage session wrote 1201-1217 and re-scoped
  1017; no code changed. Next: WP-1201.
- **2026-08-25** — [1201](../wp/1201-gui-house-style.md) shipped: one token
  layer and nine control registers in `gui/src/app.css`, `gui/CLAUDE.md`
  § House style as the vocabulary, and `gui/src/lib/style.test.ts` as the
  guard. Measured on the final tree: `font-size` declarations 69 across 15
  files → 34 across 10, seven literal sizes → the three tokens, eleven files
  declaring `.small`/`.tiny` → none. The plan named eight registers; `.pick` —
  a button that has given up its box because the row is the target — was
  written out by hand in four panels and had to be named, or the style test is
  satisfied by dropping `button` from a selector. Applying the vocabulary was
  not tidying: a multi-phase model's phase nav drew every phase in the primary
  register, so nothing showed the selection; the 3D viewer's four view buttons
  were four primaries in one row; and a chip and a pill each *contained* a
  button. A browser pass found three more that jsdom structurally cannot see,
  all cascade — a state selector keeps every property a more specific rule does
  not restate (every tab strip rendered as a segmented group),
  `text-transform` inherits into a button, and two panels put a size back at
  the call site through `font: inherit`. Given up on purpose: `⚠ not a fit yet`
  no longer routes to the Report tab, because a chip is a fact and does not
  act. Next: WP-1204.
- **2026-08-25** — [1202](../wp/1202-help-corpus.md) shipped: one place where
  a name is explained, and 92 explanations in it. Before this nothing in the
  tree said what a refinable parameter *is* — not the profile width terms, not
  the displacement parameters — and the GUI's only help was hover text kept in
  two hand-maintained TypeScript lists. `src/rietx/help.py` now holds 38
  parameter families over 49 globs plus six named arms, each crossed against
  its live vocabulary in **both** directions, so a new member fails a test and
  a rename cannot leave an entry behind describing a name that is gone.
  `unit` and `default` are the schemas' own; `typical` is the only authored
  field. Two departures from the WP's design line, both measured: a row carries
  `help_key`, the family glob, rather than the entry, because inlining an entry
  repeats a paragraph once per atom (3.4× the `/api/params` payload, 20.8 kB →
  70.0 kB on the NAC example, against 40.7 kB for the whole registry fetched
  once); and it is filled in `Refinement.parameters` rather than in the GUI
  session, so `None` means "no family claims this path" for every caller
  instead of "nobody looked". Three of the WP's own findings were wrong and the
  live vocabulary corrected them: `extinction` is a bare scalar,
  `preferred_orientation.r` is missing from the committed fnmatch corpus (whose
  two models carry no PO block, so it cannot be the coverage authority), and a
  third table path renaming existed that nobody had found — an ADP component
  loses its `aniso` segment — surfaced only because the test asserts its rename
  map is exhaustive. The manual gained a glossary generated at build time, and
  every entry's anchor is checked against the built HTML rather than the
  sources. Next: WP-1203.
- **2026-08-25** — [1204](../wp/1204-developer-mode-example-projects.md)
  shipped: developer mode, and three example projects in the wheel. The
  finding underneath both halves is that **opening a project already writes to
  it** — `Project.open` appends a head annotation to `history.jsonl` before any
  verb is called — so there was no read-only way to look at one, and every
  developer poking at a checked-in project dirtied it. `rietx gui --scratch`
  opens a byte-for-byte copy in a temp dir and names the *source* in the boot
  line; `--state-dir` exposes what only the tests could reach; the wizard's
  suggested path left the working directory, whose stated argument ("the only
  place a user has already pointed at") holds for someone who typed the command
  there and fails for a checkout. The examples are the empty state's second
  list, and each one **is** a `viz/compare.py` standard, so a protocol is never
  restated and `list_examples()` is that registry filtered by what shipped.
  What shrank the plan was a licence, not a size: `qarr/*.prn` carry no
  explicit licence and are already out of the sdist, so the four round-robin
  pure phases cannot ship, and fluorapatite had to be *created* as a standard —
  mirroring `test_acceptance_fap` field for field — to give the set a third
  specimen. `Standard` gained `pattern` and `reader_options` on the way, because
  "which file is the measurement" was a convention and a pdCIF's block choice
  changes the pattern. Measured: wheel 2.20 → 2.79 MB, examples 2.90 MB
  uncompressed; +39 python tests, +3 vitest. The browser again found what jsdom
  cannot: `.pick` gives up its box because the row is the target, so a list that
  gives the row no hover reads as prose that happens to be clickable. Next:
  WP-1202/1203.
- **2026-08-26** — [1203](../wp/1203-help-popover.md) shipped: the GUI has one
  mechanism for explaining a name, and a countable list of where it does not
  reach yet. A click on almost any term opens one popover with the entry's
  title, description, unit, the schema's own default, a typical range and a
  link into the manual; the cursor is the whole mark, which was the decision
  at the milestone opening. It is keyboard-reachable, which `title=` never was
  — and one message had been *out* of reach since WP-1201 correctly moved the
  header's "not a fit yet" badge off a button and onto a chip, a debt that WP
  recorded and this one pays. Two of this WP's own written decisions were
  overturned by measuring first. Its non-goal said "no help content", but the
  task it also carried — move the Peaks search form's 21 explanations into
  corpus keys — was impossible because WP-1202 had covered no search setting,
  so the `search_fields` arm landed here with the form's own prose **moved**
  rather than rewritten. And its planned guard, "grep for `title=` on a
  non-button and expect none", would have deleted working behaviour: of 151
  attributes, 58 are on buttons and 18 of the remaining 93 are not authored
  prose at all — 6 reveal a value a narrow column truncated, 12 carry a
  sentence the *server* wrote about one row. So the rule became what a title
  may *be*, and the guard a counted per-file budget that fails both ways: 51
  remain, describing report statistics, series settings, indexing result
  columns and drawing thresholds, which are vocabularies `rietx.help` has no
  arm for and which arrive with WPs 1208-1217. Two structural rules came with
  it. A key is `arm:name` and never a bare name, because the arms' vocabularies
  overlap (`seed` is a stage field *and* a search control) and a bare key would
  answer by declaration order — the same failure `help_key_for`'s
  exactly-one-family test rules out a rank down. And an entry's `anchor` became
  `page.html#heading-id`: WP-1202 had left this WP the choice, an id alone
  names no page, and all thirty were on exactly one page of the built manual,
  so the change was mechanical and the test got *stronger*. The browser found
  what jsdom structurally cannot, and it was this WP's own: a flex item's
  automatic minimum is its content size, so the parameter row's value input was
  floored at 152 px against a declared 92, the row's fixed columns wanted 372 px
  of a 340 px sidebar, and the path column was handed **zero** — invisible while
  it was only text, and an unclickable control the moment it became one.
  Measured: fast suite 2980 passed / 72 skipped (`[dev]`, darwin/arm64), +7
  python tests and +10 vitest, corpus 92 → 113 entries. Next: WP-1205.
- **2026-08-26** — a `/code-review` of 1203 found the accessibility repair had
  an accessibility regression, and a second browser pass measured it off the
  CDP accessibility tree: the trigger's default `aria-label="explain"` renamed
  every control that computes its own name from its contents. Accname descends
  into the term and takes the label instead of the text, so the instrument
  editor's inputs were named `explain (mm)` and `explain (deg²)` rather than
  `radius (mm)` and `U (deg²)`, the atom table's four headers `explain` rather
  than `X Y Z`/`OCC`/`BISO`/`ANISO`, the cell edges `what a is`, and the plan
  blurb's whole sentence `what the … plan does`. The WP's own subject,
  inverted, on every labelled field it touched — and invisible to jsdom, which
  has no accname, and to the browser pass that had looked at *placement*. The
  fix is that a term declares no name: its children are the name, which is what
  a `<label>` wanted anyway, and `aria-haspopup="dialog"` was already carrying
  "an explanation opens" in the channel that takes no part in a name. Seven of
  the eight `label=` props were restatements of the trigger's own text and are
  gone; the survivor is the held row's `🔒`/`=`/`·`, the one term whose children
  name nothing and the one nothing encloses. Beside it, the popover now takes
  focus — it is a `role="dialog"`, and Tab from an activated term had been
  going to the next input in the row, leaving `in the manual →` reachable only
  by tabbing the whole app. It focuses with `preventScroll`, since
  `onscrollcapture` closes the popover and a focus that scrolled would close
  what it just opened. A third finding did not survive measurement: the boot
  race that would let `place` flip on a stale height needs a term activated
  while `api.help()` is in flight, and `onMount` awaits the corpus before
  `loadProject`, so holding `/api/help` for 15 s in Chromium produced no term
  to click — the effect declares the dependency anyway, so the boot order stops
  being load-bearing, and the comment says that rather than claiming a bug.
  Also: the key gate's `for="([^"{}]+)"` had silently skipped all five
  interpolated keys, which are what a vocabulary rename breaks. Measured: fast
  suite 2984 passed / 72 skipped (`[dev]`, darwin/arm64) — +3 vitest
  (441 → 444) and no python test, the +4 being WP-1134's, landed on `main`
  under this branch.
- **2026-08-26** — [1205](../wp/1205-new-project-open-browse-defaults.md)
  shipped: the empty state can now open any project on the machine, not only
  the ones this build already knows about (`GET /api/fs`, confined to the
  home directory and the process's cwd, symlink-safe), and `Browse.svelte` is
  one modal doing that and picking the new-project directory both. The
  reported wizard bug was a real structural defect, not a missed edge case:
  `Model` was mounted twice in `App.svelte` — the empty-state wizard and the
  Model tab were two separate component instances, each its own
  `wizardOpen` — so opening a *different* project from the tab instance's
  own wizard left it painted over the freshly-opened one, because `project`
  stayed truthy the whole time and Svelte never tore the stale instance
  down. `Model` now mounts exactly once; a real-browser pass
  (playwright-core, cached chromium-1223) reproduced the exact regression
  shape live and confirmed it settles. Three client literals moved behind
  the help popover or were deleted (a pattern's `sniff`/`sigma`, a
  now-redundant reader-option `.help` span, a paraphrase of the server's
  "instrument is required" refusal) and replaced with what the wizard
  actually assumed; the scan picker states its count and default instead of
  implying them, with labels fetched the moment a multi-scan file stages.
  Measured: `tests/test_gui_server.py` 143 passed (+6); `npm --prefix gui
  test` 448 passed (+4, was 444); `npm --prefix gui run check` clean;
  `tests/test_gui_dist.py` passes (`[dev]`, darwin/arm64). Next: WP-1206.
- **2026-08-26** — [1206](../wp/1206-typed-cell-project.md) shipped: a project
  needs a lattice, not a structure. Step 2 of the wizard now takes a
  space-group symbol and a cell, builds the Le Bail scaffold the indexing
  panel's Adopt button already lands, and creates a project with no CIF
  anywhere in it. The design moved off the WP's own line in one place, and it
  is the whole of what makes the form honest: the plan said "six fields", and
  six fields is what the *old* six-number route did — under `P 4/m m m` a typed
  `b` is tied away by `ParameterTable` and the number read back was never the
  one entered. So the wire and the form carry only what the **setting** leaves
  free (`crystallography.symmetry.free_cell_names`, served by the new
  project-free `GET /api/spacegroup`), which is WP-1014's coordinate-DOF rule
  one parameter family over — unrepresentable rather than refused — and TOPAS's
  `Tetragonal(a, c)` macro shape, checked as prior art before committing to it.
  A parameter the symmetry determines is refused rather than tolerance-checked,
  because a tolerance on a *length* would be a constant nothing else here
  needs. `mode="rietveld"` is refused rather than overridden: Adopt overrides
  because there the caller chose a candidate, and here they chose a mode.
  The scaffold itself became one function
  (`schemas.structure.lebail_scaffold`, with `DUMMY_SPECIES` moved beside it
  and re-exported from `indexing.workflow`), with `structure_from_candidate`
  keeping only what a *candidate* adds and pinned bit-identical by a
  `model_dump` equality — the candidate's six numbers deliberately do **not**
  go through `complete_cell`, which would move every stored cell in the
  indexing acceptance suite at the 1e-14 level for nothing. A real-browser pass
  (playwright-core, cached chromium-1223) found four defects jsdom cannot
  reach, and three of them are about *drawing a form* rather than about this
  feature: `.segmented` is `display: flex` and stretched to the whole column in
  a block parent (every other use in the app sits in a flex row already); the
  cell boxes' numeric placeholders read as values somebody had filled in, which
  is why the instrument form's say `default`, a word; `holds: {constraints}`
  became `holds: every cell parameter is free` under a triclinic symbol; and
  step 2 marked itself done on the symbol resolving, with two empty boxes
  beside the green rule. Measured (`[dev,jax]`, darwin/arm64): a project typed
  as `P m -3 m` / a = 4.160 against the synthetic LaB6 pattern fits Le Bail to
  Rwp 0.0414, GoF 0.79, `a` back at 4.15660 ± 5e-4; fast suite 3072 passed / 72
  skipped, +10 python tests; `npm --prefix gui test` 454 passed (+6, was 448);
  `npm --prefix gui run check` clean; `tests/test_acceptance_indexing.py` 44
  passed in 19m50s (run alone), the guard the indexing rulebook asks for before
  closing anything near an engine. A review round then found four real defects on the
  PR, one of them a class rather than a slip: a new `structure` form added at
  `_as_structure` — the one boundary every model crosses — is thereby accepted
  by *every* verb crossing it, and `PATCH /api/structure` was taking the typed
  cell and leaving a `rietveld` project refining a dummy carbon. The other three
  were unphysical numbers reaching too far: `Parameter` carries no positivity
  rule, so `a = 0` validated and would have died at stage compile, and
  `NaN`/`inf` came back as a 500 with an empty `where` instead of a refusal
  naming its field — the wizard's own `blocked()` had been stricter than the
  server. Final tree 3074 passed / 72 skipped. Next: WP-1207.

- **2026-08-26** — [1207](../wp/1207-pattern-only-project.md) shipped: a
  pattern is already a project. `Project.create(structure=None)` builds one
  with zero phases, and the audit that opened the WP is the part worth
  carrying: **nothing crashed**. Every `phases` consumer in the core is
  iteration-driven — `enumerate(model.phases)`, `range(len(...))` — so at zero
  phases they do nothing and the pipeline runs to the end, returning
  **`converged` at Rwp 0.9637, GoF 23.89** against a pattern with 36 clear
  peaks, with `MODEL_FAR_FROM_DATA` firing as a diagnostic beside a status that
  contradicts it. `Structure._nonempty` was the only guard there was, so
  lifting it without a refusal on the verb would have shipped exactly the
  confident wrong answer WP-1076 is about. The audit was *measured* rather than
  read, by dropping the validator from `__pydantic_decorators__` and
  force-rebuilding every model that embeds a `Structure` — both halves needed,
  since `RefinementState` revalidates (so a subclass does not get through) and
  an embedding model inlines the compiled core schema (so rebuilding
  `Structure` alone changes nothing).
  So the refusal is on the **verb**, never the schema: a validator would refuse
  the *state* along with the fit and take peak picking and indexing with it,
  which are the only routes out. `NoPhasesError` (`code = "NO_PHASES"`) is
  raised by `fit`, `run_stage`, `refine_multi`, `refine_sequential` and the
  GUI's `POST /api/run`, each before a tree is built or an event emitted —
  the placement the zero-stage plan already used. `refine_sequential` refuses
  once up front rather than per pattern, or the ladder would read the first
  raise as a divergence to escalate against; the GUI refuses in the route
  rather than the worker, because a run *started* is a 200 whose failure only
  ever reaches the event stream. `NO_PHASES` is a **fourth agent envelope
  code** rather than an `INVALID_REQUEST`: the request is well-formed and the
  model is legal, so re-checking field spellings finds nothing and retrying
  reproduces it — what is missing is a cell.
  Two empty containers the audit found are absences now. `compute_qpa`
  returned a full `QuantitativePhaseAnalysis` with no rows because zero phases
  fell through `len(phases) > 1` into the **single-phase** branch ("one phase
  is 100 % whatever its scale did"); `geometry_table` returned an empty table
  from a function that already answers `None` outside Rietveld. `ticks` `{}`,
  the absent `PHASE_UNCONSTRAINED`, `reflection_table` `[]` and the FitReport's
  `immature` abstention with 36 unmatched peaks were already right.
  In the GUI, `StructureSource` gains `"none"` and step 2 a third
  `.segmented` button sending `structure: null` — an **answer**, where the
  key's absence stays a refusal, since `dict.get` cannot tell them apart and
  only one of the two should create a phase-free project quietly. It is the one
  route that moves the mode nowhere (with no phase there is nothing to govern,
  and Adopt sets it on the way out). `project_doc` gains `n_phases` and
  `moved()` reloads it, so Run, Run-one-stage and the series command disable
  with the reason wherever the last phase arrives or leaves.
  `PATCH /api/structure` with `{"phases": []}` became a 200 by consequence —
  WP-1206's rule that a change at `_as_structure` reaches every verb crossing
  it — and on reading it that is right: taking a wrong phase back out lands in
  the state the wizard's third route creates, and the run is what refuses.
  A real-browser pass (playwright-core, cached chromium-1223) found **no**
  defects, unlike 1206's four, and confirmed four things jsdom cannot see: Run
  is genuinely disabled (computed opacity 0.45, cursor default) carrying the
  reason as its tooltip — the downscaled screenshot had read as a solid blue
  button, and measuring is what settled it; the live transition holds in
  **both** directions with no reload (a CIF replace enables Run and restores
  the 3D column, a checkout back to the phase-free root disables it again);
  the three-button `.segmented` fits its parent, 195 of 386 px; and the Peaks
  panel works fully over a phase-free project, which is the route out.
  `SCHEMA_VERSION` 0.8 → 0.9 and `PROJECT_FORMAT_VERSION` 1.2 → 1.3, each with
  its reason beside the constant: no key moved in either, and what grew is the
  set of legal documents. Measured (`[dev,jax]`, python 3.12.12, darwin/arm64):
  fast suite **3097 passed / 72 skipped** on the merged tree (3093 before the merge, +4 for main's own new reader tests; the branch base ran 3087, so the +6 is this WP's), +6 python tests and +4 vitest (454 →
  458); `npm --prefix gui run check` clean, `ruff` clean, sphinx `-W` clean,
  dist rebuilt at `dd5807dda669`. Next: WP-1208.
- **2026-08-27** — [1208](../wp/1208-plan-introduction.md) shipped: the Plan
  panel says what a plan will *do*. `GET /api/plan/resolve` resolves each
  stage against the live parameter table and the panel draws it as a ladder —
  the paths a stage's globs reach, the ones it frees on top of the last
  (`+4 → 11 free`), the ones it cannot free carrying the row's own
  `held_because`, and the Rwp of the history node that ran it. The resolution
  runs `ParameterTable.set_vary` **itself**, on `_prepare_table(restore=False)`
  — the table `fit` builds — plus `_run_stage`'s own `mode_fixed_path` drop, so
  the tied/locked skip, the free-cell wavelength rule and the intensity mode's
  force-fix are the shipped ones rather than a second copy; `lib/fnmatch.ts`
  stays the parameter table's bulk-edit preview and matches nothing here.
  Two facts came out of building it, both of which were in the code and in
  nothing a user could read. **A fit replaces the vary flags a person set; a
  single stage continues them** — `set_vary("phases.*.atoms.*.biso")` then
  `fit(plan="profile_only")` refines no biso, while `run_stage` on the same
  state refines both — which is why `Run all` and `Run this stage` are named
  beside each other and why `set_aside` exists. `Refinement.fit`'s own comment
  had said the opposite since the method was written; it is now what the
  measurement showed. And **the per-stage Rwp was already on disk**: a stage
  node's cached statistics are bit-identical to the WP-1058 rung's, both being
  `_build_result` over the model the stage compiled and the θ it landed on, so
  the run verb did not have to grow `stage_reports=True` to show them. Measured
  on the 4200-channel synthetic LaB6, five stages, best of three: history
  off/reports off 0.075 s, reports on 0.577 s; history on 0.076 s and 0.582 s.
  A tree costs ~0.001 s (the statistics are computed for the node anyway); the
  trajectory costs **7.7×** the fit. The node is matched to a rung by position
  **and** by carrying that stage's name and `turn_on`, so an edited plan, a
  stage run from a state no plan describes, or a checkout leaves the number
  absent rather than printing a neighbour's.
  Beside the ladder: the explainer and the preset's `description` are visible
  (`when_to_use` moved into the popover), and the advanced boxes became a loop
  over `lib/rxt.ts`'s stage words rather than four typed-out fields — which
  brings `ftol`, `restraint_weight_scale` and `window_slack_deg` into the form,
  reachable only through the `.rxt` document before. `STAGE_INT_WORDS` and
  `STAGE_NULLABLE_WORDS` join `STAGE_WORDS` there, all three pinned to
  `StageSpec` from `tests/test_textdoc.py`, so a new schema field arrives in
  both surfaces keyed to its own corpus entry.
  The browser pass (playwright-core, cached chromium-1223, four passes on the
  11-BM NAC example) found **one** defect jsdom cannot see: freeing a
  parameter blanked every Rwp, because a `set_vary` node sits between the head
  and the fit's stages and the walk stopped at the first non-stage node.
  `_RWP_TRANSPARENT` names the three parameter-surface kinds that may sit
  there; `edit_model` stays opaque, since a number measured on a replaced
  model describes nothing on screen. It also settled two registers: `Run all`
  is a ghost, because the app header's Run is the same verb and two filled
  buttons read as two actions, with `Save plan` taking the fill while there is
  something to save. Cold, the NAC ladder reads `+8 → 8`, `+1 → 9`, `+2 → 11
  free · 10 held`, `+1`, `+4`, `+8 → 24 of 95`; fitted, the same column carries
  79.62 → 60.57 → 11.56 → 11.43 → 9.71 → 9.32 %. `gui/CLAUDE.md` takes four
  rules and its cap moves 710 → 733. Measured (`[dev]` only — no jax, no torch
  — python 3.12.12, numba 0.67.0, darwin/arm64) on the tree merged with
  `origin/main` at `d6f36c69` (PR #144's strain/size soft cap, which touches
  `ParameterTable.bounds` and not the free set): fast suite **3091 passed / 117 skipped** (3080 without this WP's eleven),
  +11 python tests and +3 vitest (458 → 461); `npm --prefix gui run check`
  clean, `ruff` clean, dist rebuilt at `6d896a0b0871`.
  A post-close `/code-review` round found two defects in this WP's own code,
  both reproduced before their fixes were taken: the ladder read the **first**
  run of a plan rather than the last (two back-to-back fits leave 2N
  contiguous stage nodes with nothing to mark the seam, and every other test
  here runs the plan once), and both run buttons ignored zero phases, in the
  very state this WP's own test puts the panel on screen for. The second
  repair had spelled the refusal a second time against the shell's own, so
  the panel takes `noPhasesReason` and `App.svelte` stays the one authority.
  Next: WP-1209.
- **2026-08-27** — [1209](../wp/1209-peaks-table-numbers-flags.md) shipped:
  the peak table reads like a peak table. Intensities as `I/Imax × 100` over
  the strongest *measured* line (`—` under `no_intensity`/`fit_failed`), 2θ at
  four places with the esd in the last place, `use` in its own column, seven
  columns in all; every flag chip a corpus term saying `HelpEntry.label` —
  the second authored field, held to three words and unique across the two
  chip arms — with the popover restoring the name a label hid. The origin
  chip got its own arm, `peak_origins`, keyed by `ObservedPeak.origin` both
  ways, and the panel's authored-title budget went 12 → 10. Two rules
  reach past the panel: `esdSwallowsValue` (esd ≥ 1 and larger than the
  value) makes every table write `35.09 ±110` where `35(111)` was printed
  and leaves `12346(56)` alone; and the picker's notes fold into a count.
  Measured on the final tree (`[dev]`, python 3.12.12, darwin/arm64): fast
  suite **3138 passed / 117 skipped** (+2 python tests), vitest 461 → 467,
  `svelte-check` and `ruff` clean, dist at `be85b9b15f3f`. The browser pass
  on a corundum project (built from the compare standard; no corundum
  *example* exists under the `qarr` fence) found what jsdom cannot: the
  intensity cell wore the search form's `.num`, `display: inline-flex`, and
  the browser merged it with the flex `td.flags` beside it into one
  anonymous cell — chips under the number in a column headed by nothing.
  It also overturned the triage's threshold: 0.1° printed one real line's
  position bare (σ = 0.1048°, asymmetric) beside a neighbour's `(875)`, while
  51 of 62 esds sit under 0.01° and the two degenerate ones at 1e17° and
  1e49°; the widest FWHM is 0.35°, so the boundary is a degree.
  Next: WP-1210.
- **2026-08-27** — [1210](../wp/1210-peak-layer-identity.md) shipped: the peak
  layer stops borrowing. Three reported complaints — markers that could not be
  hidden, fitted peaks "not clearly explained", and the picked-peak fit line
  and the model "both the same colour" — were one defect, and the last was
  exactly rather than approximately true: the layer had no colours of its own
  and took `--accent` and `--bad`, which on the light theme *are* `--plot-diff`
  and `--plot-calc` to the last digit (0.000 apart in OKLab). It now has
  `--plot-peak` and `--plot-peakfit` in all three theme blocks, one hue family
  (334°) in two tones with the fit curve dashed and named in the legend and the
  hover; an unusable marker is the layer's own ink drawn hollow, because state
  rides on the mark rather than on a second colour — the first attempt spent
  one on it and review measured the result 0.032 from `--plot-obs` on dark, the
  ink of the very points those markers sit on. It is drawn **only on the Peaks tab**, where a marker can be edited,
  with its two toggles listed and disabled carrying the reason elsewhere; and a
  `data only` button clears the plot to the measured points and restores the
  picture — not everything — on the second press. `tests/test_gui_palette.py`
  holds every plot colour to the phase palette's 0.13 OKLab floor, in Python
  because `structure3d._oklab_distance` is the one distance this package has,
  and it names the two pairs the shipped set already misses (light bkg/diff
  0.129, dark obs/diff 0.124) instead of exempting them quietly. The finding to
  carry: **the plot's free hue space is nearly spent** — violet is 0.10-0.12
  from `--plot-diff`, `--warn` 0.053 from `--plot-calc`, magenta is now taken,
  and green is what is left, so the next mark should plan to spend no colour at
  all. Measured on the merged tree (`[dev]`, python 3.12.12, darwin/arm64;
  `origin/main` moved under the branch mid-session, PR #118): fast suite
  **3153 passed / 117 skipped**, of which the branch's own run was 3148 / 117
  against 1209's 3138 / 117 — the ten `test_gui_palette.py` adds, no new skip;
  vitest 467 → 475 on the branch and 476 merged; `svelte-check` and `ruff`
  clean, dist at `1926c16de396`. The merge conflicted only in the built dist,
  which main had rebuilt too — resolved by rebuilding from merged sources. The
  browser pass on the fitted NAC example found two harness truths rather than
  app defects — a served session holds no result on open, so the fit must run
  *through* the server, and `POST /api/settings` takes `{"ui": {"theme": …}}` —
  and one thing left unrepaired because it predates the WP: a hidden residual
  keeps its subplot's domain, so `data only` leaves a quarter of the plot
  empty, filed into WP-1212. Next: WP-1211.
- **2026-08-27** — [1211](../wp/1211-candidate-overlay.md) shipped: a
  candidate's claim, drawn over the pattern that has to answer it. Until now
  "does this cell predict lines the pattern lacks" — the reading root
  CLAUDE.md attaches to `predicted_but_absent` — had exactly one form in the
  GUI, the `absent` column, and a count cannot say *where*. Selecting a
  candidate row now draws that cell's predicted positions as full-height lines
  through the data with everything else cleared away, served by `GET
  /api/index/ticks?candidate=` (a query string, not the WP's path segment,
  because `server.ROUTES`'s own comment says the surface has no path
  parameters). Three things the one-line design did not carry and the code had
  to get right: the **instrument's** zero shift does not belong on these lines
  (indexing fits the metric to the peak list's raw 2θ, so the cell already
  reproduces observed positions) while the **candidate's** own shift template
  does, inverted, since `refine_candidate` fits to `2θ_obs − c·T(θ)`; the
  symbol is `structure_from_candidate`'s absence-free lattice group, quoted
  rather than restated, so what is drawn is what the Le Bail validation was
  scored against; and there has to be a cap, because `max_d_axis` admits a
  25 Å triclinic cell predicting **92 103** Laue-unique lines over 5-120° at a
  Cu doublet (460 649 over 1-60° at a synchrotron λ) against 124 for corundum
  and 426 for the fap example's own top candidate. `MAX_CANDIDATE_TICKS` is
  2000, thinned **by rank in 2θ** with `n_total` printed beside it — density is
  what the picture is read for, and a head-of-list cut would leave the
  high-angle half empty, which reads as "this cell predicts nothing there".
  Four presentation decisions, each with its reason: the overlay is **not** a
  `CurveToggle`, because a toggle is a second control for a thing whose control
  is the row and pressing it would leave a row looking selected with nothing
  drawn (the status line under the plot says what the toggle row would have);
  the clear to data-only goes through the `data only` button's own press, one
  saved list and a flag saying whose press it was, since two slots is four
  interleavings of two presses and no rule a reader could state; **a preview is
  not a selection**, which is two props, because driving the clear off "is
  something drawn" made the model strobe once per row the pointer crossed; and
  full height is an overlaying `yaxis4` rather than shapes (which join the x
  autorange, WP-1033, and re-lay-out on every drag) or the tick band (a tick
  states a fitted model's position; this is a hypothesis over the data).
  `--plot-candidate` is green — the last of the free hue space WP-1210
  measured, so the plot palette now has room for no further mark carrying a
  quantity — at 0.150 (light) and 0.168 (dark) from its nearest neighbour
  against the 0.13 floor, and `test_gui_palette.py`'s peak-*pair* assertions
  generalised to a `LAYERS` tuple so a third layer inherits every one of them.
  The browser pass ran on the **fap** example rather than corundum, which
  cannot be an example project at all (`qarr/*.prn` carry no licence statement,
  WP-1204) and would have been the worse subject anyway: a Cu doublet makes
  "every emission line" visible as pairs sitting on the peaks. It found the one
  defect jsdom structurally could not — 426 lines over 115° is ~3.7 per pixel
  at the survey view, and drawn last they buried the pattern completely, so the
  overlay was hiding the one thing it exists to be compared with; drawn first
  it is a wash under the data, which is also the honest order. Three self-review
  repairs went in on top, none found by a test: an in-flight fetch could land
  after a renumbering (`candidateEra`, the text pane's stale-`seq` rule one
  panel over), `structure_from_candidate` could raise past the error envelope,
  and the thinning turned 2001 lines into 1001 before the picks were spaced
  evenly over the ranks. Then `/code-review medium --fix` found two more, and
  the first is the sharper lesson of the WP: **a selection is instant and its
  answer is not**, so keying the clear on "a row is selected" took the model
  curves off before there were lines to replace them — a flash on a slow
  enumeration, and on a *refused* cell a plot cleared to the data with nothing
  drawn and no sentence saying why, since the status line does not render
  either. That is the preview-is-not-a-selection rule one step earlier than
  where it was caught the first time; the second finding was a row the pointer
  crossed twice before its answer landed being fetched twice. The pass ran late,
  after the suite and after the PR, because the session was served a **stale**
  `/wp-handover`: commands are discovered once from the launch directory, and
  the main checkout was nine merges behind the `origin/main` this branch came
  from, so its copy predated the commit that added the review step. The
  worktree's copy is the authority. Left undone and retargeted rather than left
  rotting:
  `lib/help.test.ts`'s `title=` budget line named this WP as the owner of the
  candidate and extinction tables' nine authored tooltips, which it is not —
  1211 added no `title=` at all — and describing them needs a corpus arm over
  `ExtinctionCandidate`/`CellCandidate` `model_fields`, which is where `ΔBIC`,
  `testable`, `refuting` and `absent` actually live and is a
  `peak_origins`-sized commit of its own. Measured (`[dev]`, python 3.12.12,
  darwin/arm64; the branch is a fast-forward from `origin/main`, so these *are*
  the merged tree's, and all re-measured after the review's fixes): fast suite
  **3157 passed / 117 skipped** against 1210's 3153 / 117 — the four
  `candidate_ticks` adds, no new skip, unchanged across the fixes, which touched
  no python — vitest **476 → 487**, `svelte-check` 378 files 0 errors, `ruff`
  clean, dist at `4708ce7d593a`. One wall clock worth keeping because it is the
  §Quoting-numbers rule measured rather than asserted: the identical fast
  selection ran **2:18** idle and **4:21** while a `/pr-review` suite held the
  cores, 1.9× from load alone. Next: WP-1212.
- **2026-08-27** — [1212](../wp/1212-redraw-never-moves-axes.md) shipped: **a
  redraw never moves the axes**, which turned out to be one sentence in WP-1044
  that had looked complete. `heldRanges` kept an axis only once
  `autorange === false`, and plotly writes that flag on a zoom and nowhere else,
  so on the plot a person had *not* zoomed there was nothing to keep and every
  redraw and every `restyle` re-fitted the axes — which is why the same gesture
  measured clean on a zoomed plot and the report kept coming back. The axes are
  now made explicit by the paint that fitted them (`pinPatch`, run before the
  hover ring goes on), and the two things the flag used to mean are two
  functions: `movedAxes` reads each gesture off the relayout event, `userRanges`
  says what survives the re-fit a new payload licenses. Measured on the unzoomed
  NAC example, in Chrome: hover, unhover, a tab change, arming and a whole
  exclude drag now move nothing, where each moved `yaxis` before — a hover by
  1.03 % of its span, once per row the pointer crossed, through a `restyle` that
  cost no `react` at all. The exclude drag went 4 reacts → 1: arming is a
  `relayout` because the drag mode is one layout key (two of the four, set on
  the way in and cleared on the way out), `extent` is keyed by value because a
  `$derived` off `project` arrives new-but-equal on every settings PATCH, and
  `setProtocol` reads the peak list before publishing either assignment because
  two `$state` writes either side of an `await` are two flushes. The WP's own
  near-miss is the durable lesson: pinning read `_fullLayout.xaxis.range`, and
  on the **raw** view — no fit yet, which is where peaks are picked and cells
  indexed — that field was still plotly's empty-axis default `[-1, 6]` while the
  axis drew 0-60°, so the pin froze a blank plot. Three measurement passes missed
  it because all three ran a fit first, and a fit re-fits the axes anyway; it was
  found by looking at a screenshot of a state the table did not cover, and
  settled by checking `main`'s dist, where the picture is right and `range` reads
  `[-1, 6]` just the same. The range plotly draws with is `ax._rl`. Two defects
  came out of the same gesture and left with it: a select drag threw inside
  plotly once per pointer move (an *empty* `scattergl` trace gets no index in the
  scene its peers share, so `selectBatch[undefined].length` threw — the hover
  ring is a plain `scatter` now, 7 throws over three drags → 0), and the live
  selection is dressed as the exclusion it is about to make, its wash a
  `.select-outline` rule carrying `!important` because plotly writes
  `fill-opacity: 0` inline. Two inherited questions answered rather than passed
  on twice: the candidate hover costs 11 reacts at 19-33 ms over six rows and
  moves no axis (a cost question, not a correctness one), and the residual
  subplot's empty quarter is **decided against** collapsing here — the shared x
  axis is anchored to `y2` and the tick band's domain is the gap between the
  panels, so it is a layout redesign, and with nothing on it plotly drops
  `yaxis2` and restores it with exactly its previous range. The review pass
  raised four findings against the WP's own new code and fixed all four, of
  which one is real (a drag on Δ/σ surviving as a range on Σχ² — the clear is a
  pure function now, because the first test written for it passed with *and*
  without the fix), one is this WP's own bug on the one view it had not covered
  (the raw view's double-click left the axes autoranging, since its relayout
  handler returns before any repaint), one is a guard whose defect does not
  reproduce in Chrome, and one is a guard on a path a browser could not reach —
  each recorded as which. Measured (`[dev]`, darwin/arm64): vitest **487 →
  510**, `svelte-check` 378 files 0 errors, fast python **3157 passed / 117
  skipped** — exactly 1211's, since no python behaviour moved and the three
  suites that could have are inside that count — `gui/CLAUDE.md` 808 → 838 lines
  with its reason beside the cap. Next: WP-1213.
