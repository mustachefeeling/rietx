"""Episode builders for the agent-surface round.

An episode is a workspace an agent is handed: data files, and nothing that
encodes the maintainer's intended workflow.  E-ZRM's files are a third party's
and are fetched, never committed (PROTOCOL.md § The episodes); E-RAMP's are
simulated here, so the episode is rebuilt rather than kept.
"""
