// Present so vite-plugin-svelte stops reporting that it defaulted.
// Svelte 5 runes are on by default and the plugin handles TypeScript in
// <script lang="ts">, so there is nothing else to configure.
export default {};
